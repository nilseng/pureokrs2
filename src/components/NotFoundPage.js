import React from "react";

const NotFoundPage = () => {
  return (
    <div
      style={{
        textAlign: "center",
        marginTop: "40vh",
        fontSize: "2rem",
        fontWeight: 200
      }}
    >
      Sorry, can't find the page :´(
    </div>
  );
};

export default NotFoundPage;
